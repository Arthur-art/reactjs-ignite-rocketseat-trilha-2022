import { createStitches } from '@stitches/react'

export const {
  config,
  globalCss,
  getCssText,
  css,
  styled,
  keyframes,
  theme,
  createTheme,
} = createStitches({
  theme: {
    colors: {
      rocketseat: '#8257e6',
    },
  },
})
